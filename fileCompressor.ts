// Multi threaded file compressor
//  Compress large files using multiple threads—each thread compresses a chunk, and the results are merged.

export function compressFile(fileToCompress: Express.Multer.File) {
    
}
